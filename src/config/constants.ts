export const CANVAS_CONFIG = {
  WIDTH: 1920,
  HEIGHT: 1080,
  TARGET_FPS: 60
} as const

export const HEXAGON_CONFIG = {
  ROWS: 10,
  BORDER_WIDTH: 3,
  CELL_GAP: 12
} as const

export const STARS_CONFIG = {
  STAR_COUNT: 300,
  MIN_SIZE: 0.5,
  MAX_SIZE: 2
} as const